<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="FoodHub.View.RestaurantPage"
             xmlns:local ="clr-namespace:FoodHub.ViewModel" Title="Home">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <StackLayout.BindingContext>
                <local:RestaurantViewModel/>
            </StackLayout.BindingContext>
            <Frame Margin="0, 0, 0, 2" Padding="0" OutlineColor="DarkGray" HasShadow="True" HorizontalOptions="FillAndExpand"  VerticalOptions="Center">
                <SearchBar x:Name="SearchBar" Placeholder="Search Restaurant" HorizontalOptions="FillAndExpand" VerticalOptions="Center"/>
            </Frame>
            <ListView x:Name="RestaurantListView" BackgroundColor="WhiteSmoke" ItemsSource="{Binding MyRestaurantList}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}" TextColor="Yellow" Detail="{Binding ID}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>